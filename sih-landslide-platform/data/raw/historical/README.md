Drop real historical landslide event files here as `.geojson` or `.csv`.

CSV columns required: latitude,longitude,date,confidence

Until real files are added, the historical service reports
`source: "no historical dataset loaded"` and returns a seeded placeholder
susceptibility value — it never pretends this is real event data.
